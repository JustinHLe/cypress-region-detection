{
  "name": "applied-cypress-region-testing",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:version": "yarn cypress --version",
    "cy:open": "yarn cypress open",
    "cy:run:chrome": "yarn cypress run --headed --browser chrome",
    "cy:run:firefox": "yarn cypress run --headed --browser firefox",
    "cy:run:edge": "yarn cypress run --headed --browser edge"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.4.1",
    "yarn": "^1.22.17"
  },
  "devDependencies": {
    "cypress-each": "^1.11.0"
  }
}
